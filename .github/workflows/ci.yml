name: CI

on:
  push:
    branches: [main, feature/terrain-stage2]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install test and geospatial dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test,geo]"

      - name: Compile package
        run: python -m compileall -q app qanat

      - name: Import smoke test
        run: python -c "import app.main; import qanat.pipeline; print('Qanat import smoke test: OK')"

      - name: Run tests
        run: python -m pytest -v
